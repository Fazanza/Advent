#include <fstream>


using namespace std;

int main() {
    int curMaximum = 0;
    int curValue = 0;

    ifstream ins;
    ins.open("input.txt");
    while(getline(ins, curValue)){

    }
    
    return 0;
}